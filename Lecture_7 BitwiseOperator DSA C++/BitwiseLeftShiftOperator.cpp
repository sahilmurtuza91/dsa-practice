#include<iostream>
using namespace std;
int main(){
    cout<<"Perform bitwise left shift operator(<<)";
    int x=10;
    
    cout<<(x<<2); // this will b=shift the the two digit of binary number of 10 so that the answer will become 40 in decimal form
}