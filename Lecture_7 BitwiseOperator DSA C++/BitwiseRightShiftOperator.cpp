#include<iostream>
using namespace std;
int main(){
    cout<<"Perform bitwise right shift operator(<<)"<<endl;
    int x=10;
    
    cout<<(x>>1); // this will shift the the one digit of binary number of 10 to its left side so that the answer will become 5 in decimal form
}